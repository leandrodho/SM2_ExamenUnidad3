name: proyecto_safearea
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  firebase_core: ^4.2.0
  shared_preferences: ^2.5.3
  cloud_firestore: ^6.0.3
  firebase_auth: ^6.1.1
  firebase_storage: ^13.0.3
  provider: ^6.1.5+1
  firebase_messaging: ^16.0.3
  flutter_local_notifications: ^18.0.1
  google_maps_flutter: ^2.9.0
  google_maps_flutter_web: ^0.5.7
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  http: ^1.2.2
  image_picker: ^1.1.2
  geolocator: ^13.0.1
  flutter_launcher_icons: ^0.14.4
  fl_chart: ^0.68.0
  intl: ^0.19.0
  csv: ^6.0.0
  share_plus: ^7.0.0
  


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  change_app_package_name: ^1.1.0

flutter_launcher_icons:
  android: true
  image_path: "assets/safearea_2.png"

flutter:
  uses-material-design: true
